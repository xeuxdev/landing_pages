export default function Blurred({ classNames }: { classNames: string }) {
  return (
    <div
      className={`h-[17.80919rem] w-[17.80919rem] md:w-[35.80919rem] md:h-[34.53031rem] blurred rounded-full ${classNames}`}
    />
  )
}
